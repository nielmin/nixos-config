{
  den.hosts.x86_64-linux = {
    asta.users.daniel = { };
    # host provides default home environment for its users
    # homeManager =
    #   { pkgs, ... }:
    #   {
    #     home.packages = [ pkgs.vim ];
    #   };
  };
}
